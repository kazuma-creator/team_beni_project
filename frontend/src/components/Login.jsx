import React,{useState} from 'react';

const Login = () =>{
  const [userId,serUserId] = userState('');
  const [password,setPassword] = userState('');

  const handleLogin = () =>{
    //ログイン処理
  };
  const handleRegister = () =>{
    //新規登録処理
  };
}